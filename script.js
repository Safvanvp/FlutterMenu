// JS placeholder for future enhancements like filtering or dynamic pricing
console.log("Welcome to Flutter Cafe!");
document.querySelectorAll('.card').forEach((card, index) => {
    card.style.animationDelay = `${index * 0.1}s`;
});

